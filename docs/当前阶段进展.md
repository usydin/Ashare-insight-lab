# 当前阶段进展

## V0.7.2 - Data Source Status UI (当前)

- **核心状态**：增强桌面端前端 UI 对数据源状态（在线、缓存兜底、过期缓存、失败）的显式呈现。
- **已完成项**：
  - [x] Dashboard DataHealthPanel 接入 cache_fallback / stale_cache 统计与说明。
  - [x] TopBar 增加数据源状态动态提示。
  - [x] MarketIndexView / SectorBoardView / WatchlistView 增加数据源状态 Badge。
  - [x] ReportCenterView 摘要统计与复核说明。
- **待处理项**：
  - [ ] 后续版本：接入 Tushare 作为第二数据源候选。

## V0.7.1 - Local Cache Fallback

- **核心状态**：实现本地 raw CSV 缓存兜底能力。
- **已完成项**：
  - [x] 新增 `local_cache_provider.py`，支持查找与加载 raw CSV 缓存。
  - [x] `DataSourceManager` 实现 AkShare 失败后的自动降级链。
  - [x] 区分新鲜缓存 (`cache_fallback`) 与过期缓存 (`stale_cache`)。
  - [x] `run-daily` 接入并记录降级状态。

## V0.6.7 - 品牌资产接入与桌面端视觉统一

- **核心状态**：品牌资源标准化与 UI/UX Polish 完成。
- **已完成项**：
  - [x] 接入标准化 logo, banner, favicon。
  - [x] Sidebar 视觉升级与导航逻辑分组。
  - [x] Dashboard 首页品牌 Hero 区域。
  - [x] 统一全局复制反馈与空状态样式。
  - [x] 确认 Tauri macOS 图标 (src-tauri/icons/icon.icns)。
  - [x] 更新 README 与开发文档。
- **待处理项**：
  - [ ] 后续版本：集成 AI 分析摘要（预留区域已就绪）。
  - [ ] 后续版本：完整 SQLite 历史查询支持。

## V0.3.1

- 完成 V0.1.3 数据源健康检查与网络诊断
- 完成 V0.1.4 TradingAgents 源码架构学习与 Agent 适配规划
- 完成 V0.1.5 FinceptTerminal 产品形态与终端架构研究
- 完成 V0.1.6 MiroFish 多智能体情景推演研究
- 完成 V0.1.7 三项目融合路线总览与长期架构收敛
- 完成 V0.2.1 Watchlist 扩展与字段增强
- 完成 V0.2.2 A股指数数据接入
- 完成 V0.2.3 行业/板块基础数据接入
- 完成 V0.2.4 UI 技术路线冻结与产品形态规划
- 完成 V0.3.0 SQLite 本地数据资产基础层
- 完成 V0.3.1 历史查询与变化摘要
  - 实现 `app_core/analytics/history_summary.py` 对比模块
  - 新增 `python3 app.py changes` 命令
  - `run-daily` 自动写出 `signal_changes.csv`
  - 建立 `docs/data/历史信号变化摘要说明.md`
- 新增 5 份核心架构文档与 4 份 UI 规划文档
  - `docs/architecture/A股智研台长期架构总览.md`
  - `docs/architecture/模块分层与职责边界.md`
  - `docs/architecture/三项目融合路线图.md`
  - `docs/architecture/V0.2功能路线说明.md`
  - `docs/architecture/安全边界与许可证总原则.md`
- 明确未来 UI 与金融终端化技术路线（PySide6 + Python Core）
- 更新项目追踪文档：README.md, 当前阶段进展.md, 开发计划.md, AI协作规则.md, 版本记录.md, 开源金融项目借鉴总览.md
- 完成 V0.1 run-daily 主流程
- 支持 `python app.py` 与 `python app.py run-daily`
- 接入 AKShare 日线采集
- 输出 raw CSV、增强版 `daily_signals.csv`、Markdown 日报与 `logs/app.log`
- 支持 MA5 / MA20 趋势信号
- 支持单只股票失败不中断整轮流程
- 支持 `Ctrl+C` 优雅中断
- 支持 watchlist 扩展字段、默认值标准化与 CSV 元信息保留
- `pytest` 已通过当前回归测试
- 新增统一项目元信息管理
- 明确开发者为 `pL`
- 明确版权品牌为 `@B‘lock10STUdio`
- 新增 `check-data-source` / `doctor` 数据源健康检查命令
- 新增代理环境、依赖可用性与示例股票采集诊断报告

## 当前进行中

- 继续推进 V0.2 数据源与日报增强
- 暂不进入模拟盘、AI Agent、GUI、OpenClaw、Scenario Lab 代码实现

## 当前未做

- 未实现 GUI 代码
- 未实现模拟盘撮合与记账
- 未实现多 Agent 角色代码
- 未实现 DataHub 代码
- 未做 OpenClaw 代码
- 未做部署机正式落地

## 当前判断

项目已完成研究阶段向实现阶段的切换，当前正式进入 V0.2 的第一步：从 watchlist 配置与日报字段增强入手，逐步把数据层和报告层做厚，同时继续坚持最小闭环、结构化输出和安全边界优先。
