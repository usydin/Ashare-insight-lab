# 当前阶段进展

## V1.0-alpha-03-longbridge-oauth-quote-market-support (当前)

### 当前最新提交

- `c4d1dc0 feat: add realtime source status and switch prototype`

### 当前阶段能力

- 实时行情源状态控制台
- Token 本地安全管理
- Longbridge 只读接入研究
- Marketaux 国际新闻源
- React 前端状态展示
- UI snapshot 同步
- Longbridge OpenAPI 支持排查材料沉淀
- 新增独立 OAuth Client ID 支持（LONGBRIDGE_OAUTH_CLIENT_ID）

### 细化能力说明

- 已支持 AkShare A 股实时行情 `quote`。
- 已支持 AkShare / 本地 Provider `kline` 能力。
- 已支持 `quote`、`quote-batch`、`kline`、`source-status` CLI。
- 已支持 Marketaux Token 管理与国际新闻 CLI。
- 已支持本地 Token 管理：`token-status`、`token-set`、`token-clear`。
- 已支持 `.env` 自动加载与 `.secrets` metadata。
- 已完成 Longbridge 只读行情基础层、legacy 状态识别、本地 OAuth token 准备层、OAuthBuilder 研究命令和失败诊断。
- 已将 `source_status` 纳入 `ui_snapshot`。
- 已完成 RealtimeMarketView 的行情源状态与切换视觉原型。
- 已完成 SettingsView 的 API Token 状态 / 数据源状态分区展示。

### 当前进展

- Longbridge OAuth 已授权成功（浏览器显示 Authorization Successful）。
- 根因回溯：过去误用 App Key 作为 OAuth client_id，引发 internal_server_error；已通过独立 OAuth Client ID 修复。
- SDK token cache 由 Longbridge SDK 托管，`longbridge-oauth-status` 对应显示 `sdk_managed_configured`。
- `longbridge-oauth-quote` 已支持 US/HK 只读行情映射（AAPL.US / 700.HK）；A 股根据账号行情权限再评估。
- 仍不启用 Longbridge 为默认行情源，保持只读验证阶段。

### 已验证

- `pytest 197 passed`
- `frontend-react` build 成功
- `ui_snapshot` 生成链路可用
- `validate-snapshot` 可通过
- `longbridge-sdk-status`、`longbridge-status`、`longbridge-oauth-help` 可运行
- `longbridge-status` 增加 oauth_client 区块（client_id present/missing、redirect_uri、app_key_is_oauth_client_id=false）
- `longbridge-oauth-start` 仅读取 LONGBRIDGE_OAUTH_CLIENT_ID，缺失时返回 oauth_client_id_missing；授权时自动打开浏览器且脱敏输出
- `longbridge-oauth-status` 在 SDK 托管 token 且本地无明文 token 时，显示 `sdk_managed_configured`
- `longbridge-oauth-quote` 支持 US/HK 市场映射与只读输出，不包含敏感信息
- 密钥扫描无输出
- `TradeContext` 未导入 / 未调用

### 当前安全边界

- 只读行情
- 不接交易
- 不读资产 / 持仓 / 订单
- 不下单 / 撤单 / 改单
- token 不写入 Git / logs / reports / snapshot / frontend json
- `.env` / `.secrets` / `data` / `logs` / `reports` / `dist` / `target` 应保持 ignored

### 下一步建议

- 联系 Longbridge OpenAPI 支持
- 使用 OAuth Client ID 重新执行 `python3 app.py longbridge-oauth-start`
- Longbridge OAuth 问题解决后再进入前端真实行情卡片接入
- 前端行情源切换交互增强
- 数据源健康评分
- Tushare 接入评估
- OpenAI Key 后续用于 AI 摘要 / 研报
- Tauri 桌面包阶段演示

## 里程碑回顾

### V0.9.7 - realtime source status and switch prototype

- 新增 `source-status` 多数据源状态摘要。
- `ui_snapshot` 已纳入 `source_status`。
- RealtimeMarketView 已增加行情源状态与切换视觉原型。
- SettingsView 已区分 API Token 状态与数据源状态。

### V0.9.6.1 - OAuth internal_server_error diagnostics

- 增强 Longbridge OAuthBuilder 授权失败时的脱敏诊断输出。
- 已确认当前失败阶段位于授权服务端或 OAuth 配置侧。

### V0.9.6 - OAuthBuilder quote research commands

- 新增 `longbridge-sdk-status`、`longbridge-oauth-help`、`longbridge-oauth-start`、`longbridge-oauth-quote`。
- 完成 Longbridge 只读行情 OAuthBuilder 研究命令。

### V0.9.4 / V0.9.5 - Token 与 Longbridge 只读准备层

- 完成本地 Token 管理能力与 `.env` 自动加载。
- 完成 Longbridge 只读接入准备与 OAuth 本地 token 准备层。

## 当前判断

项目已完成从“单一数据采集 + 日报链路”向“多数据源状态透明化 + 只读行情控制台 + 前端可展示原型”的阶段演进。当前最适合把 Longbridge OAuth 授权失败问题整理成正式支持排查材料，在获得 Longbridge 支持结论前，不继续扩展前端实时行情卡片或硬接真实 Longbridge 行情。
